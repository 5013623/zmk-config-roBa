
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # ZMK 本体は main の最新（Editor と整合しやすい）
      zmk-ref: main
      # Zephyr は ZMK main が現在使っている安定ブランチに固定
      zephyr-ref: v3.5-branch
